# modules/ethernet.yaml
# Logika zarządzania siecią i bezpieczeństwem WiFi

interval:
  - interval: 60s
    then:
      - lambda: |-
          if (!network::is_connected()) {
            ESP_LOGE("network", "Brak połączenia LAN!");
          }
