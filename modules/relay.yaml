switch:
  - platform: gpio
    name: "Gateway Relay"
    id: relay1
    pin:
      number: ${relay_pin}
    restore_mode: ALWAYS_OFF
    icon: "mdi:power-socket-eu"

#  - platform: gpio
#    id: relay2
#    name: "Relay 2"
#    pin: ${relay2_pin}

